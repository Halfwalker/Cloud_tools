---

dependency:
  name: galaxy
driver:
  name: docker

platforms:
  - name: "instance-${MOLECULE_DISTRO:-ubuntu2004}"
    image: "geerlingguy/docker-${MOLECULE_DISTRO:-ubuntu2004}-ansible:latest"
    command: ${MOLECULE_DOCKER_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: true
    pre_build_image: true

provisioner:
  name: ansible
  env:
    GH_USER: ${GH_USER}
    GH_TOKEN: ${GH_TOKEN}

verifier:
  name: goss


---
kind: pipeline
type: docker
name: default

trigger:
  branches:
    - master
  event:
    - push
    - merge

steps:
  - name: Lint
    image: geerlingguy/docker-ubuntu2004-ansible
    commands:
      - apt update
      - apt install --no-install-recommends yamllint
      - yamllint .


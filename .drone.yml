---
kind: pipeline
type: docker
name: default

trigger:
  branches:
    - master
  event:
    - push
    - merge

steps:
  - name: Lint
    image: quay.io/ansible/toolset
    commands:
      - yamllint .
      - ansible-lint --force-color

